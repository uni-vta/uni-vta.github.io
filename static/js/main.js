const nav=document.getElementById('siteNav'),progress=document.getElementById('progressBar'),toggle=document.getElementById('navToggle'),links=document.getElementById('navLinks');function onScroll(){nav.classList.toggle('scrolled',scrollY>12);const total=document.documentElement.scrollHeight-innerHeight;progress.style.width=`${total>0?scrollY/total*100:0}%`}addEventListener('scroll',onScroll,{passive:true});onScroll();toggle.addEventListener('click',()=>{const open=links.classList.toggle('open');toggle.setAttribute('aria-expanded',String(open))});links.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>links.classList.remove('open')));const revealObserver=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('visible');revealObserver.unobserve(entry.target)}}),{threshold:.08});document.querySelectorAll('.reveal').forEach(el=>revealObserver.observe(el));const videoObserver=new IntersectionObserver(entries=>entries.forEach(entry=>{const video=entry.target;if(entry.isIntersecting){video.play().then(()=>video.parentElement.classList.add('playing')).catch(()=>{})}else{video.pause();video.parentElement.classList.remove('playing')}}),{threshold:.55});document.querySelectorAll('.scroll-video').forEach(video=>{videoObserver.observe(video);video.parentElement.addEventListener('click',()=>{if(video.paused)video.play().then(()=>video.parentElement.classList.add('playing'));else{video.pause();video.parentElement.classList.remove('playing')}})});
